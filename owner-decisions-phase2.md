# Owner decisions for phase 2

Authoritative decisions recorded on 2026-07-12:

1. Phase 1 is complete as an accepted architecture contract.
2. Phase-2 roles must be mature enough for the first demo; deliberately weak stencil implementations are not accepted.
3. The meaning-changing checkpoint belongs on a dedicated dashboard/widget surface.
4. The live agent pool belongs in Ouroboros project workpad/state and is edited through project context, not by out-of-band task-folder overrides.
5. Routine `outline.json` is assembled by the orchestrator LLM under a fixed storyline contract, not by a separate deterministic compiler.
6. `presentation-outline/v1` is versioned before storyline execution.
7. A2A stays disabled until a real remote peer exists; an executable endpoint/redirect guard becomes mandatory before enabling that peer.
8. Accepted routine learning is reused through namespaced Ouroboros knowledge/scratchpad entries.

Engineering follow-ups such as exact schemas, self-checks, manifest annotations, and binding refreshes are implementation decisions derived from these owner choices. They must not be quoted as additional owner mandates.
